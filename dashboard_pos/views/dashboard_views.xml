<odoo>
    <data>
        <record id="action_pos_dashboard" model="ir.actions.client">
            <field name="name">Dashboard</field>
            <field name="tag">pos_dashboard</field>
        </record>

        <record id="inherited_pos_order_tree" model="ir.ui.view">
            <field name="name">inherited.pos.order.tree</field>
            <field name="model">pos.order</field>
            <field name="inherit_id" ref="point_of_sale.view_pos_order_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='date_order']" position="after">
                    <field name="date"/>
                </xpath>
            </field>
        </record>

        <menuitem id="menu_pos_dashboard" name="Détails de caisse" parent="point_of_sale.menu_point_rep" sequence="1"/>

        <menuitem name="Indicateur des ventes" id="pos_dashboard"
                  sequence="1"
                  parent="dashboard_pos.menu_pos_dashboard"
                  groups="point_of_sale.group_pos_manager"
                  action="action_pos_dashboard"/>
    </data>

</odoo>
